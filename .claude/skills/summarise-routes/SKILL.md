---
name: summarise-routes
description: Summarise all Flask routes defined in app.py. Use when asked to list, describe, or review the routes in this project.
---

Look at the file `src/ai_workflow_lab/app.py` and provide a concise summary of every route defined inside `create_app()`. For each route, list:
- The HTTP method and path
- The function name
- A one-sentence description of what it does

Format the output as a simple markdown table.

